ROOT=$(TUP_CWD)

CXX=g++
CXXFLAGS=-std=c++1y -I$(ROOT)/include -g -Wall -Werror -pedantic
CXXFLAGS+=-isystem $(ROOT)/ext

!c++ = |> $(CXX) $(CXXFLAGS) -c %f -o %o |>
!link = |> $(CXX) %f -o %o -lpthread -lcurl |>
